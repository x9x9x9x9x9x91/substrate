# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-release/
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.worktrees/
.claude/*
!.claude/skills/
!.claude/settings.json

# Playwright
/test-results/
/playwright-report/
# evidence-only shots (SHOTS=1 specs), never product
/shots/

# TS incremental build state
*.tsbuildinfo

# npm ci freshness marker written by scripts/rig-gates-runner.sh — a local
# build artifact, and an untracked one would make the tree unreleasable
.npm-ci-stamp

# Local Cargo config is intentionally untracked. Checked-in absolute paths
# made every clone fail after the primary-machine cutover (SUB-987).
.cargo/config.toml

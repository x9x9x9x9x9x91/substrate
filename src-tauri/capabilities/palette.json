{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "palette",
  "description": "Capability for the everywhere palette window",
  "windows": ["palette"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:event:allow-listen"
  ]
}

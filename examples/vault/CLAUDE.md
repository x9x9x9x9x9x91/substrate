Read `AGENTS.md` in this folder before working — it is the canonical guide to
this vault: what Substrate is, the note/frontmatter format, dashboards and
sheets, and what not to touch. This pointer exists because some agents
auto-load `CLAUDE.md` but not `AGENTS.md`; the content lives there, once.
